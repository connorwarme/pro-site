---
const { title, description, url, date, type } = Astro.props;
---

<li class="bloglist-item" id={type}>
  <a href={url}>
    <h4>{title}</h4>
    <p>{description}</p>
    <p>{date}</p>
  </a>
</li>
<style>
  li {
    list-style: none;
    padding: 0 0 20px 0;
  }
</style>