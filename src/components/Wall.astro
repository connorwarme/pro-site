---
const { climb, grade, pitches, style, location } = Astro.props.climb;
const pitchesText = pitches ? `, ${pitches} pitches` : '';
const styleText = style ? `, ${style}` : '';
---
<div>
  <p><em>{climb}</em></p>
  <p>{grade}{pitchesText}{styleText}</p>
</div>
<style>
  div {
    margin-bottom: 1rem;
  }
  p {
    margin: 0;
  }
</style>